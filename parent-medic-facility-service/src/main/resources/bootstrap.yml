spring:
  cloud:
    config:
      uri: ${config-server.host}:8000
  application:
    name: parent-medic-facility-service
  r2dbc:
    url: r2dbc://localhost:3306/parent-medic-facility

server:
  port: 8004