# =============================== SPRING CFG ===========================================
spring:
  application:
    name: orangtua-service
  cloud:
    config:
      uri: http://${config-server.host}:8000

# =============================== SERVER CFG ===========================================
server:
  port: 8002
  error:
    include-message: always