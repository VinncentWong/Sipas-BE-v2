# =============================== SPRING CFG ===========================================
spring:
  application:
    name: gateway-server
  cloud:
    config:
      uri: http://${config-server.host}:8000
  main:
    web-application-type: reactive

# =============================== SERVER CFG ===========================================
server:
  port: 8001